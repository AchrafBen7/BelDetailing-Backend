services:
  api:
    build: .
    container_name: beldetail-api
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env          # 👈 très important
    environment:
      NODE_ENV: development
    command: npm run dev
